# Asterion EDA

Asterion EDA is a browser-based electronic design automation workbench built with plain HTML, CSS and JavaScript, with a GPU-first rendering architecture and local-first project workflow.

## Live application

https://wieslawsoltes.github.io/AsterionEDA/

## Verified standalone build

The root `index.html` is reconstructed and verified byte-for-byte by CI before deployment.

- Size: `251624` bytes
- SHA-256: `e0d9a93bba0b2456a031d87ec57dda683e8a368077c8ce58a9761bc52f8936d9`

The `payload/` directory contains the XZ/base64 provenance payload used to reproduce the exact standalone application. GitHub Pages is deployed only after both checksum and size validation succeed.
