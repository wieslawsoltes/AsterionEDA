# Asterion EDA

Asterion EDA is a browser-based electronic design automation workbench built with plain HTML, CSS and JavaScript, using WebGPU when available with a Canvas fallback.

This repository contains the full runnable source, examples, tests, documentation and a standalone single-file build.
